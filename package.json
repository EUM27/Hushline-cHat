{
  "name": "hushline-chat",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "check": "corepack pnpm -r run check",
    "build": "corepack pnpm -r run build",
    "test": "corepack pnpm --filter @hushline/server test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.30.3"
}
