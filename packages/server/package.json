{
  "name": "@hushline/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "check": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@hushline/shared": "workspace:*",
    "hono": "^4.10.7",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3"
  }
}
